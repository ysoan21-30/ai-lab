# --- Core LLM / RAG stack ---
anthropic>=0.40.0
chromadb>=0.5.0
streamlit>=1.38.0

# --- Embeddings (API-based via Voyage AI, Anthropic's recommended provider —
# no local model, so no onnxruntime/torch pulled in) ---
voyageai>=0.3.0
tiktoken>=0.7.0

# --- Data handling ---
pandas>=2.2.0
numpy>=1.26.0

# --- Document loading (fraud reports, PDFs, CSVs, etc.) ---
pypdf>=4.3.0
python-docx>=1.1.0

# --- Config / env ---
python-dotenv>=1.0.1
pydantic>=2.8.0

# --- Dev / testing ---
pytest>=8.3.0
black>=24.8.0
